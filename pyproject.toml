[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "marketlens"
version = "0.1.0"
description = "Market Intelligence & Strategic Decision Platform"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["marketlens*"]
